
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: INSECURE - For debugging only.
    // Allows any authenticated user to read/write to any document.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
