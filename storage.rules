rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // The app does not use Storage, so deny all access by default.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
